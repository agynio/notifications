1. The notifications service has been deployed.
2. Verify the service status:
   kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}
3. The gRPC endpoint is exposed on port {{ .Values.service.port }} via the {{ include "notifications.fullname" . }} service.
